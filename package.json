{
  "private": true,
  "scripts": {
    "security:pentest": "bash scripts/run_zap_baseline.sh",
    "test:contract": "python3 scripts/check_public_contract.py && npm --prefix web test && go test -C backend ./... -count=1"
  }
}
