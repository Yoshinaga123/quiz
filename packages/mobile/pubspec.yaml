name: quiz_mobile
description: Native quiz application built with Flutter.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  equatable: ^2.0.7
  flutter_riverpod: ^2.6.1
  flutter_local_notifications: ^21.0.0
  shared_preferences: ^2.5.5
  flutter_secure_storage: ^9.2.2
  app_links: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
